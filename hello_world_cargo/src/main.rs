fn main() {
    // cargo build (compile and create binary, pass the --release flag to optimize the code)
    // cargo run (compile and execute)
    // cargo check (compile, use to check fast for errors)
    println!("Hello, world!");
}
